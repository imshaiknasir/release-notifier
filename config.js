/**
 * Configuration for the GitHub Release Notifier
 */

module.exports = {
    // List of repositories to monitor in 'owner/repo' format
    REPOS: [
        'microsoft/playwright',
        'SeleniumHQ/selenium',
        'google-gemini/gemini-cli',
        'anthropics/claude-code',
        'VibiumDev/vibium'
    ],

    // You can add other settings here in the future
};
